Program is run via the WordCount.java file

Program takes in a file via Scanner object

File is passed to wordCounter method which stores each line in word and then splits line via different regex symbols, storing each separation as an array element

Words in file are counted as a total, and as specific cases, in this instance cast speakers in the script.

